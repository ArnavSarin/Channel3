import { SvgIcon } from '@mui/material';
import Channel3 from './svg/Channel3Logo.svg';
import { IconProps } from './types';

export const Channel3Icon = (props: IconProps) => {
    return <SvgIcon component={Channel3} inheritViewBox {...props} />;
};
